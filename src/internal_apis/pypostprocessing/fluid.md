# Fluid
*As of version 3.0.36*<br>
`FLUID` can be used like so:
```lua
FLUID{
  type = "fluid",
  name = "example-fluid"
}
```
You can also use it to get existing fluids
```lua
FLUID("example-fluid")
```
